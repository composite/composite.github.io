<style>
  .intro {
    @apply absolute left-0 right-0 top-0 bottom-0 shadow-2xl shadow-gray-900 flex flex-col justify-center items-center text-4xl text-white;
    z-index: 1;
    background-image: url('/intro.jpg');
    background-size: cover;
    background-attachment: fixed;
  }
  .intro::before {
    @apply absolute;
    content: "";
    top: calc(50% - 16vw);
    left: calc(50% - 16vw);
    width: 32vw;
    height: 32vw;
    background-color: rgba(0, 0, 0, 0.5);
    border-radius: 9999px;
    z-index: -1;
  }
  .intro p {
    background-color: rgba(0, 0, 0, 0.5);
  }
  .intro h1 {
    font-size: max(6vw, 2rem);
    margin-bottom: clamp(1rem, 3vw, 3rem);
  }
  .intro h2 {
    font-size: max(3vw, 1.2rem);
  }
</style>
<div class="intro">
  <h1 class="font-bold">Fullstack</h1>
  <h2>business developer</h2>
  <p class="absolute bottom-0 left-0 right-0 text-center text-base">
    아래로 스크롤하십시오.
  </p>
</div>
